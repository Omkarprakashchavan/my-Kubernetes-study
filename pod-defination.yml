---
apiVersion: v1

kind: Pod

metadata:
  name: redis
  namespace: default
  labels:
    app: nginx
    tier: db

spec:
  containers:
    - name: nginx-alpine-container
      image: nredis:alpine


apiVersion: v1

kind: Pod

metadata:
  name: redis
  namespace: finance
  labels:
    app: redis
    type: backend

spec:
  containers:
    - name: redis-container
      image: redis